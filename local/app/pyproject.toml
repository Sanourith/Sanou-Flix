[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "sanouflix"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "sqlalchemy",
  "pandas",
  "psycopg2-binary",
  "python-dotenv"
]
