grafana:
  enabled: true
  adminPassword: "changeme"   # à passer en variable sensible
  service:
    type: NodePort
    nodePort: 30300

prometheus:
  prometheusSpec:
    retention: 15d
    resources:
      requests:
        memory: "512Mi"

alertmanager:
  enabled: false   # à activer quand tu en auras besoin
